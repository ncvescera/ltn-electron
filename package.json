{
  "name": "ltn-electron",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "html": "cp -r ./src/gui/* dist/ && cp -r ./src/gui/icons dist",
    "start": "npm run build && npm run html && electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^17.1.2",
    "typescript": "^4.6.2"
  }
}
